import { viewports, argTypes } from './parameters';

export const parameters = {
    viewport: {
        viewports,
    },
    controls: {
        expanded: true,
    },
    argTypes: {
        ...argTypes,
    },
};
